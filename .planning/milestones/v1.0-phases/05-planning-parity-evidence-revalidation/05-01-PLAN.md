---
phase: 05-planning-parity-evidence-revalidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/phases/05-planning-parity-evidence-revalidation/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-planning-workflow-migration/03-SUMMARY.md
autonomous: true
requirements:
  - TOOL-02
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "Parity test suites under src/tests/parity/ execute as part of default npm test"
    - "TOOL-02 has explicit requirement-ID evidence in a verification artifact"
    - "PLAN-01 has explicit requirement-ID evidence in a verification artifact"
    - "PLAN-02 has explicit requirement-ID evidence in a verification artifact"
    - "REQUIREMENTS.md traceability rows for TOOL-02, PLAN-01, PLAN-02 reference Phase 5"
  artifacts:
    - path: "package.json"
      provides: "test:unit script includes src/tests/parity/*.test.ts glob"
      contains: "src/tests/parity/*.test.ts"
    - path: ".planning/phases/05-planning-parity-evidence-revalidation/05-VERIFICATION.md"
      provides: "Requirement-ID evidence for TOOL-02, PLAN-01, PLAN-02"
      contains: "TOOL-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability rows"
      contains: "Phase 5"
  key_links:
    - from: "package.json"
      to: "src/tests/parity/*.test.ts"
      via: "test:unit script glob"
      pattern: "src/tests/parity/\\*\\.test\\.ts"
    - from: "05-VERIFICATION.md"
      to: "src/tests/parity/"
      via: "requirement evidence rows"
      pattern: "TOOL-02|PLAN-01|PLAN-02"
---

<objective>
Wire parity test suites into the default npm test regression gate and create explicit requirement-ID evidence for the three orphaned requirements (TOOL-02, PLAN-01, PLAN-02) identified by the v1.0 milestone audit.

Purpose: The milestone audit found these three requirements mapped to Phase 3 but absent from all verification and summary frontmatter. Parity suites exist and pass (36 Phase 3 + 23 Phase 4 tests) but are not in the default test path, reducing regression protection. This plan closes both gaps.

Output: Updated package.json with parity glob, 05-VERIFICATION.md with requirement evidence, updated REQUIREMENTS.md traceability, updated Phase 3 summary frontmatter.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-planning-workflow-migration/03-VERIFICATION.md
@.planning/phases/04-parity-validation-safe-switchover/04-VERIFICATION.md

<interfaces>
<!-- Current test:unit script (package.json line 56): -->
"test:unit": "node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test src/resources/extensions/gsd/tests/*.test.ts src/resources/extensions/gsd/tests/*.test.mjs src/tests/*.test.ts"

<!-- Parity test files (not currently in test path): -->
src/tests/parity/planning-parity.test.ts         — 12 tests (Phase 3)
src/tests/parity/plan-check-equivalence.test.ts  — 11 tests (Phase 3)
src/tests/parity/e2e-planning-parity.test.ts     — 23 tests (Phase 4)

<!-- Phase 3 test:integration already picks up: -->
src/tests/integration/planning-surfaces.test.ts  — 13 tests (streaming parity)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire parity suites into default npm test path</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Add `src/tests/parity/*.test.ts` to the `test:unit` script glob in package.json.

Current value:
```
"test:unit": "node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test src/resources/extensions/gsd/tests/*.test.ts src/resources/extensions/gsd/tests/*.test.mjs src/tests/*.test.ts"
```

New value (append parity glob at end):
```
"test:unit": "node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test src/resources/extensions/gsd/tests/*.test.ts src/resources/extensions/gsd/tests/*.test.mjs src/tests/*.test.ts src/tests/parity/*.test.ts"
```

This adds all 3 parity test files (46 total tests) to the default `npm run test:unit` and therefore `npm test` path.

Run `npm run test:unit` to confirm all parity suites execute and pass alongside existing unit tests.
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json `test:unit` script contains `src/tests/parity/*.test.ts`
    - `npm run test:unit` exits 0
    - Output includes test results from `planning-parity.test.ts`, `plan-check-equivalence.test.ts`, and `e2e-planning-parity.test.ts`
  </acceptance_criteria>
  <done>All 3 parity test files execute as part of default npm test:unit; exit code 0</done>
</task>

<task type="auto">
  <name>Task 2: Create requirement-ID verification evidence and update traceability</name>
  <files>
    .planning/phases/05-planning-parity-evidence-revalidation/05-VERIFICATION.md
    .planning/REQUIREMENTS.md
    .planning/phases/03-planning-workflow-migration/03-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/03-planning-workflow-migration/03-VERIFICATION.md
    .planning/phases/03-planning-workflow-migration/03-SUMMARY.md
    .planning/phases/04-parity-validation-safe-switchover/04-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
**1. Create `05-VERIFICATION.md`** with explicit requirement-level evidence:

For each orphaned requirement, map to the specific test files and verification evidence that proves satisfaction:

- **TOOL-02** ("User receives streaming output and completion events with behavior parity across TUI, headless, and web surfaces"):
  - Evidence: `src/tests/integration/planning-surfaces.test.ts` — 13 tests covering TUI subscribe/unsubscribe/parallel listeners, RPC JSON-serializable events, Web event delivery order across Pi and Copilot backends
  - Evidence: `src/tests/parity/planning-parity.test.ts` — 12 tests confirming equivalent output structure across backends
  - Cross-ref: 03-VERIFICATION.md "Streaming Parity (TUI, RPC, Web)" table — all 13 criteria pass

- **PLAN-01** ("User can run discuss/plan flows on Copilot SDK with parity to current command UX"):
  - Evidence: `src/tests/parity/planning-parity.test.ts` — discuss parity (question count variance ≤20%, topic overlap ≥60%) and plan parity (score variance ≤10%, matching verdicts)
  - Evidence: `src/tests/parity/e2e-planning-parity.test.ts` — full roundtrip E2E discuss and plan parity across backends
  - Evidence: `src/workflows/discuss-phase.ts` and `src/workflows/plan-phase.ts` — backend-aware workflow modules created in Phase 3
  - Cross-ref: 03-VERIFICATION.md discuss and plan parity tables

- **PLAN-02** ("User can run plan-check style validation in the migrated planning path"):
  - Evidence: `src/tests/parity/plan-check-equivalence.test.ts` — 11 tests: well-formed plans pass on both backends, empty plans fail on both, deficient plans produce matching issue detection
  - Evidence: `src/tests/parity/e2e-planning-parity.test.ts` — "E2E Plan-Check Equivalence" describe block (5 tests) + "E2E Roundtrip Validation" (6 tests)
  - Cross-ref: 03-VERIFICATION.md "Plan-Check Equivalence" table

Include frontmatter with `requirements_verified: [TOOL-02, PLAN-01, PLAN-02]` and `status: passed`.

**2. Update `REQUIREMENTS.md` traceability table:**

Change these rows:
- `TOOL-02 | Phase 5 | Pending` → `TOOL-02 | Phase 5 | Complete`
- `PLAN-01 | Phase 5 | Pending` → `PLAN-01 | Phase 5 | Complete`
- `PLAN-02 | Phase 5 | Pending` → `PLAN-02 | Phase 5 | Complete`

Also check the checkboxes for TOOL-02, PLAN-01, PLAN-02 in the requirements list (change `- [ ]` to `- [x]`).

**3. Update `03-SUMMARY.md` frontmatter:**

Add `requirements-completed` field to Phase 3 summary frontmatter:
```yaml
requirements-completed:
  - TOOL-02
  - PLAN-01
  - PLAN-02
```

This closes the "absent from SUMMARY frontmatter requirements-completed fields" gap identified in the audit.
  </action>
  <verify>
    <automated>grep -c "TOOL-02\|PLAN-01\|PLAN-02" .planning/phases/05-planning-parity-evidence-revalidation/05-VERIFICATION.md && grep "Complete" .planning/REQUIREMENTS.md | grep -c "TOOL-02\|PLAN-01\|PLAN-02" && grep "requirements-completed" .planning/phases/03-planning-workflow-migration/03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 05-VERIFICATION.md exists and contains explicit rows for TOOL-02, PLAN-01, PLAN-02
    - 05-VERIFICATION.md frontmatter contains `requirements_verified: [TOOL-02, PLAN-01, PLAN-02]`
    - REQUIREMENTS.md traceability table shows `TOOL-02 | Phase 5 | Complete`
    - REQUIREMENTS.md traceability table shows `PLAN-01 | Phase 5 | Complete`
    - REQUIREMENTS.md traceability table shows `PLAN-02 | Phase 5 | Complete`
    - REQUIREMENTS.md checkboxes for TOOL-02, PLAN-01, PLAN-02 are `[x]`
    - 03-SUMMARY.md frontmatter contains `requirements-completed` with TOOL-02, PLAN-01, PLAN-02
  </acceptance_criteria>
  <done>All three orphaned requirement IDs have explicit verification evidence; traceability rows show Complete; Phase 3 summary frontmatter includes requirements-completed</done>
</task>

</tasks>

<verification>
1. `npm test` passes (includes parity suites)
2. `grep -c "src/tests/parity" package.json` returns 1
3. `05-VERIFICATION.md` contains evidence rows for all 3 requirement IDs
4. `REQUIREMENTS.md` shows Phase 5/Complete for TOOL-02, PLAN-01, PLAN-02
5. `03-SUMMARY.md` frontmatter includes requirements-completed field
</verification>

<success_criteria>
- All parity test files (46 tests) run as part of default `npm test`
- Every orphaned requirement (TOOL-02, PLAN-01, PLAN-02) has explicit verification evidence
- Milestone traceability is fully satisfied for all three requirements
- Phase 3 summary frontmatter retroactively claims the requirements it delivered
</success_criteria>

<output>
After completion, create `.planning/phases/05-planning-parity-evidence-revalidation/05-01-SUMMARY.md`
</output>
